@model Figlut.Spread.Web.Site.Models.CountryModel

<div id="dlgEditCountry">
    <img src="~/Images/Icons/red-edit32.png" style="float:right; margin-right:1%; margin-left:1%" />
    <div class="centerForm">
        <form id="EditCountryForm">
            @Html.ValidationSummary(true, "Failed to edit.")
            <fieldset>
                <legend>Confirmation Form</legend>
                @Html.HiddenFor(m => m.CountryId)
                @Html.HiddenFor(m => m.DateCreated)
                <div class="field-label">
                    @Html.Raw("Country Code")
                </div>
                <div class="editor-field">
                    @Html.EditorFor(m => m.CountryCode)
                    @Html.ValidationMessageFor(m => m.CountryCode)
                </div>
                <div class="field-label">
                    @Html.Raw("Country Code")
                </div>
                <div class="editor-field-dialog">
                    @Html.EditorFor(m => m.CountryName)
                    @Html.ValidationMessageFor(m => m.CountryName)
                </div>
                <br />
                <div id="EditCountryErrorField" style="color:#8A0808"></div>
            </fieldset>
        </form>
    </div>
</div>

<script type="text/javascript">
    $(function () {
        var dialogName = '#dlgEditCountry';
        createDialog(dialogName, 'Edit Country', onConfirmOK, onConfirmOpen, onConfirmCancel, 450, 450);
    });

    function onConfirmOK() {
        var dialogName = '#dlgEditCountry';
        var postBackUrl = '@Url.Action("EditDialog", "Country")';
        $.ajax({
            url: postBackUrl,
            type: 'POST',
            data: $("#EditCountryForm").serialize(),
            success: function (data) {
                if (data.Success) {
                    $(dialogName).dialog("close");
                    //Submit the form which will refresh grid to show the change the enabled/disabled.
                    var form = $('#frmDetails');
                    form.submit()
                } else {
                    $("#EditCountryErrorField").html(data.ErrorMsg);
                }
            }
        })
    };

    function onConfirmOpen() {

    }

    function onConfirmCancel() {
        //$('#confirmation-message').val('');
        $("#EditCountryErrorField").html('');
    }
</script>